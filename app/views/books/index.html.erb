
<head>
<title>books</title>
</head>

<header>
<h1>Books</h1>
</header>

<main>

<table>
<% @book.each do |book| %>

<%=link_to post.title, post_path(post.id) %>

<%= link_to "show" , show_post_path(post) %>
<%= link_to "edit" , edit_post_path(post.id) %>
<%= link_to "Destroy", post_path(post), method: :delete %>
<% end %>
</table>

</main>

<footer>

<h1>New book</h1>

<%= form_for(@book) do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>


<% end %>

</footer>
